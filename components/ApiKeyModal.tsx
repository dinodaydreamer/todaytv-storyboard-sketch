
import React from 'react';

/**
 * ApiKeyModal is deactivated to comply with Gemini API coding guidelines:
 * "The application must not ask the user for [the API key] under any circumstances."
 * The API key is managed via the environment variable process.env.API_KEY.
 */
const ApiKeyModal: React.FC<any> = () => {
  return null;
};

export default ApiKeyModal;
